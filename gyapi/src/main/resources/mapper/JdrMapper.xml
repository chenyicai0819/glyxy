<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.guet.mapper.JdrMapper">

    <select id="getJdr" resultType="cn.edu.guet.entity.Jdr" parameterType="string">
        SELECT * FROM jdr WHERE gh=#{userId}
    </select>
</mapper>